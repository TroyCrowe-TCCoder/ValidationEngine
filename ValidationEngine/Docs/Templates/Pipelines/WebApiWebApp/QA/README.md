# QA

Reserved placeholder for a future QA environment stage. Not yet implemented in the reference pipeline (`CaptiveExpensesAPI`). When a QA branch/environment is introduced, add `QA.yml` (entry point) and stage templates here following the same pattern as `Dev/` and `Main/`.
